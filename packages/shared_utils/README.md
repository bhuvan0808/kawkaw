# shared_utils — Phase 2+

Shared utilities (validators, formatters, distance/geo helpers, currency, date) used by
the Flutter apps. A TypeScript mirror for web utilities lives in `packages/shared_types`.

> Populated alongside Phase 2.
