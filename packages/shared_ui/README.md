# shared_ui (Flutter package) — Phase 2

Shared Flutter UI kit: Kaw Kaw Material 3 dark theme, color tokens, typography,
rounded cards, large touch-target buttons, and reusable widgets used by `customer_app`
and `rider_app`.

Theme tokens: primary `#111827`, secondary `#F59E0B`, accent `#FFFFFF`, success `#10B981`.

> Populated alongside Phase 2.
