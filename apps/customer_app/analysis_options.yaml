include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    invalid_annotation_target: ignore
  exclude:
    - "**/*.g.dart"
    - "lib/firebase_options.dart"

linter:
  rules:
    avoid_print: true
