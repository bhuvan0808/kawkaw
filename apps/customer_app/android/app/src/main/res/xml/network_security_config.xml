<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic is HTTPS only. Cleartext is permitted ONLY for local
  development hosts (Android emulator host + localhost) so the app can reach a
  dev backend on http://10.0.2.2:3000. This keeps the Play Store data-safety
  posture clean while allowing local testing.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
